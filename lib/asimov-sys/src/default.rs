// This is free and unencumbered software released into the public domain.

impl Default for AsiFlowExecutionState {
    fn default() -> Self {
        Self::ASI_FLOW_EXECUTION_STATE_UNKNOWN
    }
}

impl Default for AsiPortState {
    fn default() -> Self {
        Self::ASI_PORT_STATE_OPEN
    }
}

impl Default for AsiStructureType {
    fn default() -> Self {
        Self::ASI_TYPE_UNKNOWN
    }
}
